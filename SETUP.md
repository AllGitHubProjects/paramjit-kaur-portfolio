# Quick Setup — TL;DR

You only need to run two commands the first time:

```bash
cd "C:/Users/paramjit.kaur/Downloads/My Portfolio/portfolio-react"
npm install
npm run dev
```

Open the URL Vite prints (usually <http://localhost:5173/>).

If you don't have Node.js yet, install it from <https://nodejs.org/> (LTS version).

For full instructions — building, pushing to GitHub, deploying — see **README.md**.
